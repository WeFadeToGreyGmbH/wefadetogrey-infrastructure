################################################################################
# GLOBAL GLPI BACKUP CONFIGURATION
# Backs up THE one global GLPI server for entire company
# 
# CURRENT: Remote GLPI on 10.0.3.13
# FUTURE:  Docker GLPI on 10.0.3.18 (migration planned)
################################################################################

# ============ CURRENT SETUP: Remote GLPI on 10.0.3.13 ============
DEPLOYMENT_MODE="remote"           # remote = physical server, docker = container
GLPI_HOST="10.0.3.13"              # Current: Physical server
GLPI_PORT="80"
GLPI_DB_HOST="10.0.3.13"           # Current: Database on same server
GLPI_DB_PORT="3306"
GLPI_DB_USER="glpi"
GLPI_DB_PASSWORD=""                # Use .my.cnf or set securely
GLPI_DB_NAME="glpi"

# SSH access (for remote backup)
GLPI_SSH_HOST="10.0.3.13"
GLPI_SSH_USER="root"
GLPI_SSH_KEY="/root/.ssh/id_rsa"
GLPI_ROOT="/var/www/glpi"

# ============ FUTURE SETUP: Docker GLPI on 10.0.3.18 ============
# When migrating to Docker on 10.0.3.18, change to:
#
# DEPLOYMENT_MODE="docker"
# DOCKER_HOST="10.0.3.18"
# DOCKER_CONTAINER="glpi"
# GLPI_DB_HOST="localhost"        (inside container)
# GLPI_SSH_HOST="10.0.3.18"       (for file backups)
#
# Then run: ./scripts/deploy-glpi-backup.sh setup
#
# Everything else stays the same - no code changes needed!

# ============ BACKUP STORAGE (Centralized) ============
BACKUP_DIR="/mnt/backups/glpi-global"
BACKUP_RETENTION_DAYS="90"
ENABLE_COMPRESSION="true"
COMPRESSION_LEVEL="9"

# ============ LOGGING ============
LOG_FILE="/var/log/glpi-backup/global.log"
LOG_LEVEL="INFO"

# ============ VERIFICATION ============
VERIFY_BACKUP="true"
VERIFY_INTEGRITY="true"

# ============ NOTIFICATIONS ============
ENABLE_EMAIL_NOTIFICATIONS="true"
EMAIL_ON_SUCCESS="false"
EMAIL_ON_FAILURE="true"
EMAIL_RECIPIENT="ops@company.de"
EMAIL_FROM="glpi-backup@company.de"

# ============ DISASTER RECOVERY ============
ENABLE_DISASTER_RECOVERY="true"
DR_SYNC_INTERVAL="daily"
